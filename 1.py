print(" Sanjivani university")
1. Initialize Git (if not already initialized)
sh
Copy
Edit
git init
2. Add a remote repository (only needed once per project)
sh
Copy
Edit
git remote add origin <your-repository-URL>
(Replace <your-repository-URL> with the actual GitHub repository link.)

3. Add the file(s) to the staging area
sh
Copy
Edit
git add <filename>
(For all files, use git add .)

4. Commit the changes
sh
Copy
Edit
git commit -m "Added new file"
5. Push the file to GitHub
sh
Copy
Edit
git push -u origin main


git init
git remote add origin linkpaste
git remote -v

git





